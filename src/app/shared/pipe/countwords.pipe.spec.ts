import { CountwordsPipe } from './countwords.pipe';

describe('CountwordsPipe', () => {
  it('create an instance', () => {
    const pipe = new CountwordsPipe();
    expect(pipe).toBeTruthy();
  });
});
